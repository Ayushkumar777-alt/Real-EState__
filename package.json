{
  "name": "vex-hero",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "vercel-build": "cd client && npm install && npm run build",
    "install-all": "npm install --prefix client && npm install --prefix server",
    "dev": "concurrently -n CLIENT,SERVER -c blue,green \"npm run dev:client --prefix client\" \"npm run dev --prefix server\"",
    "client": "npm run dev:client --prefix client",
    "server": "npm run dev --prefix server"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "google-auth-library": "^9.11.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.4.4",
    "multer": "^2.0.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "mongodb-memory-server": "^11.2.0"
  }
}
